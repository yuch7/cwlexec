language: java
jdk:
  - openjdk8

script:
  - mvn clean package
  - docker build --build-arg CWLTOP=$(pwd) -t cwlexec-test -f conformance/Dockerfile .
  - docker run -v /var/run/docker.sock:/var/run/docker.sock cwlexec-test

