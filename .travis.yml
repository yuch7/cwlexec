language: java
jdk:
  - openjdk8

script:
  - mvn clean package
  - docker build -t cwlexec-test conformance/
  - docker run -v /var/run/docker.sock:/var/run/docker.sock -v $(pwd):/cwlexec cwlexec-test

